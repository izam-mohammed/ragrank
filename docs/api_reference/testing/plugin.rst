ragrank_pytest
==============

The pytest plugin. It lives outside the ``ragrank`` package so that
pytest's startup import does not pull the library in; see the module
docstring for why that matters.

.. automodule:: ragrank_pytest
    :members:
